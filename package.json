{
  "name": "pr-ai-summary",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outfile ./dist/main.mjs --target=node --format=esm --minify"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@google/genai": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}